export const userValue = (state) => state.user.user;
export const isLoggedIn = (state) => state.user.isLoggedIn;
export const favoritesValue = (state) => state.favorites.favorites;
export const accessToken = (state) => state.auth.accessToken;
export const refreshToken = (state) => state.auth.refreshToken;
// export const globalLoadingValue = (state) => state.globalLoading.toggleGlobalLoading;
